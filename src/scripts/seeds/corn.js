export const createCorn = () => {
    //    create a function with an array of two objects
    const seed = [
        {
            type: "Corn",
            height: 180,
            output: 6
        },
        {
            type: "Corn",
            height: 180,
            output: 6
        }
    ]

    return seed
}